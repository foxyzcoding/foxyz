build:
	podman build -t foxyz-benchmark .

run:
	podman run foxyz-benchmark --mode headless --browser firefox
	podman run foxyz-benchmark --mode headless --browser foxyz
	podman run foxyz-benchmark --mode headless --browser foxyz-ubo
